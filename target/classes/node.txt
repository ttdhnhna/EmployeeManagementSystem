* Thứ tự những nhiệm vụ cần phải làm: 
* Để tạo liên kết giữa 2 bảng dữ liệu phòng ban và nhân viên thì mình sẽ dùng đến @onetomany và @manytoone 
cái này sẽ cần phải nghiên cứu 1 lúc mới được.(Đã thành công)

* Sau khi làm xong cái chức năng đăng nhập với đăng ký thì sẽ chuyển trang đăng nhập sẽ là trang hiển thị đầu tiên
và sau khi đăng nhập thành công thì mới chuyển đến trang chủ

* Sửa cái hiện id. (Xong)

* Chức năng bổ sung: Sắp xếp theo tên, id, ...; Phân trang; Xem chi tiết nhân viên, phòng ban; Tìm kiếm nhân viên theo id; 
Tính lương và cật nhật lương, Hiện bảng thông tin các nhân viên trong phòng ban.

* Sửa chức năng phân trang, sắp xếp và sửa phòng ban (Xong)

* Làm chức năng đăng nhập và đăng xuất cho người dùng.

* Tạo khả năng phân biệt tài khoản admin và user 

* Bổ sung chức năng phân quyền

* Tạo chức năng hiển thị lịch biểu và giao nhiệm vụ cho nhân viên.

* Tạo chức năng nhắn tin.

Ghi chú: Phần tìm kiếm trong lương thì sẽ có cả tìm mã nhân viên, mã lương và tên nhân viên
Đã thêm được phần sửa vs thêm lương ở nhân viên rồi
Giờ sẽ cần chạy thử xem việc thêm, hiện lương
Khong thay dc thuoc tinh idnv o 2 bang luong
va phong ban, phan sua ma luong o nv thi van on va hien len dc.

* Phần tìm kiếm ở trang phòng ban vẫn chưa hoạt động đc nên sẽ để tạm đấy vậy
* khi nào xử lý xong việc hiện danh sách nhân viên trong phòng ban thì sẽ bắt
* đâu xử lý.

Có thể sẽ phải update springboot version nếu vẫn tiếp tục gặp lỗi và dưới đây là cách: 
To update your Spring Boot version in your Maven project, follow these steps:

1. **Update the Parent POM:**

    Open your `pom.xml` file and locate the `<parent>` section. Update the `<version>` element to the desired Spring Boot version. For example:

    ```xml
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.6.2</version> <!-- Update to the latest version -->
        <relativePath/>
    </parent>
    ```

2. **Update Dependency Versions:**

    If you have any specific Spring Boot dependencies in the `<dependencies>` section, ensure they use the same version. You can remove the version from the individual dependencies and let them inherit the version from the parent. For example:

    Before:

    ```xml
    <dependencies>
        <!-- Other dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <version>2.6.2</version> <!-- Remove this line -->
        </dependency>
        <!-- Other dependencies -->
    </dependencies>
    ```

    After:

    ```xml
    <dependencies>
        <!-- Other dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <!-- No version specified; it will inherit from the parent -->
        </dependency>
        <!-- Other dependencies -->
    </dependencies>
    ```

3. **Clean and Rebuild:**

    Save your changes and rebuild your project. You can do this by running the following Maven commands in your project directory:

    ```bash
    mvn clean
    mvn install
    ```

    Or you can use an integrated development environment (IDE) like IntelliJ IDEA or Eclipse to trigger a clean and build.

4. **Verify the Update:**

    After a successful build, check the console or logs for any warnings or errors related to the dependencies. Ensure that your project is using the updated Spring Boot version.

By following these steps, you should be able to update your Spring Boot version in your Maven project. Make sure to test your application thoroughly to ensure compatibility with the new Spring Boot version.


Có chỉnh sửa 1 số thứ ở trong file pom nên nếu có lỗi gì sẽ cần phải chỉnh sửa lại.